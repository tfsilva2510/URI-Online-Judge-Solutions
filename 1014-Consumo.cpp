#include <bits/stdc++.h>

using namespace std;

int main(){
    int x;
    double y;
    cin >> fixed >> setprecision(1);
    cin >> x >> y ;
    cout << fixed << setprecision(3);
    cout << x / y  << " km/l";
}

